name: Capture Windows pnpm test failure

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  capture:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/actions/setup-node-pnpm
      - name: Run tests
        id: test
        continue-on-error: true
        shell: pwsh
        run: pnpm test *> pnpm-windows-test.log
      - name: Upload test log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pnpm-windows-test-log
          path: pnpm-windows-test.log
          retention-days: 1
      - if: steps.test.outcome != 'success'
        run: exit 1
